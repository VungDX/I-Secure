import { Typography } from 'antd';

const { Title } = Typography;

const Document = () => (
  <div>
    <Title level={2}>Document</Title>
    <p>Thông tin về Document.</p>
  </div>
);

export default Document;