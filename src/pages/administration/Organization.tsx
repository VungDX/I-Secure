import { Typography } from 'antd';

const { Title } = Typography;

const Organization = () => (
  <div>
    <Title level={2}>Organization</Title>
    <p>Thông tin về Organization.</p>
  </div>
);

export default Organization;