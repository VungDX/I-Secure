import { Typography } from 'antd';

const { Title } = Typography;

const NotFound = () => (
  <div>
    <Title level={2}>NotFound</Title>
    <p>Trang không tồn tại.</p>
  </div>
);

export default NotFound; // Thêm export default