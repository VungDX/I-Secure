import { Typography } from 'antd';

const { Title } = Typography;

const ViolationList = () => (
  <div>
    <Title level={2}>ViolationList</Title>
    <p>Danh sách ViolationList.</p>
  </div>
);

export default ViolationList;