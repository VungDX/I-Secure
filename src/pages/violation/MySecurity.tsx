import { Typography } from 'antd';

const { Title } = Typography;

const MySecurity = () => (
  <div>
    <Title level={2}>My Security</Title>
    <p>Đây là trang My Security.</p>
  </div>
);

export default MySecurity; // Đảm bảo có export default