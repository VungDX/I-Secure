import { Typography } from 'antd';

const { Title } = Typography;

const SecurityBoardRegister = () => (
  <div>
    <Title level={2}>Security Board Register</Title>
    <p>Bảng điều khiển bảo mật.</p>
  </div>
);

export default SecurityBoardRegister;